apiVersion: v1
kind: ConfigMap
metadata:
  name: biochubs-api-config
  namespace: biochubs
  labels:
    app: biochubs-api
data:
  # Database configuration (non-sensitive)
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "hubs_dev"

  # Application settings
  LOG_LEVEL: "info"
  WORKERS: "4"
